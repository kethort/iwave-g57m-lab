# Screenshot Inventory

| File | Contents |
| --- | --- |
| `overview.png` | Root README showcase with the application header, workflow tabs, and JTAG inputs |
| `jtag-workflow.png` | U-Boot networking, reconstructed PDI inputs, and JTAG actions |
| `qspi-workflow1.png` | QSPI configuration, custom PLM, Linux payloads, and explicit PDI components |
| `qspi-workflow2.png` | QSPI partition layout and provisioning operations |
| `execution-log.png` | Execution output, generated command preview, and resolved configuration |

The root README keeps the overview expanded and places the remaining images in
a collapsed gallery. Workflow guides display the relevant screenshots at full
width so field labels remain readable.
